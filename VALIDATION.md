# 검증 결과 · v1.0.0

검증일: 2026-09-13. 대상: 본 ZIP의 Python 엔진·API·브라우저 화면. 실제 위성 또는 Render 계정에 연결하지 않은 로컬 검증입니다.

## 자동 테스트

`python -m pytest -q`: **28 passed** (약 2.4초, 로컬 환경).

- 원궤도 주기와 적도 궤도의 1/4주기 위치를 해석해와 비교
- 500·888·1,280 km × 64·128·512기 시나리오의 위성 수·위도 범위·대전 가시성 확인
- 위 시나리오에서 CSV의 설정·온도·AI 이상지수가 API 결과와 일치
- 5종 장애 모두 관측 규칙으로 탐지, 적합한 모의 대응 후 정상 기준으로 회복
- 미대응 결과와 대응 결과의 비교 방향 확인
- JSON에 해당하는 동일 요청 재실행 결과 일치
- 서로 다른 실험 요청 및 영향을 받지 않은 위성 간 상태 독립성 확인
- 잘못된 설정·위성 번호·장애·명령·중복 대응·시각에 대한 입력 검증
- 컴퓨터 재시작에 따른 일시 서비스 중단과 이후 회복 확인
- 합성 정상 데이터의 일부 관심 알림이 숨겨지지 않는지 확인

테스트 도구의 전이 의존성에서 deprecation 경고 2개가 발생했으며 테스트 실패는 없습니다.

## 브라우저 검증

Chromium에서 데스크톱 1500px, 모바일 390px 화면을 확인했습니다.

| 흐름 | 결과 |
|---|---|
| 초기 위성군·지도·텔레메트리 표시 | 정상 |
| 과열 예제 불러오기 | 선택 위성 경고 표시 |
| 탑재체 부하 축소 → 5분 진행 | 선택 위성 정상 표시 |
| 같은 예제의 대응/미대응 온도 | 34.5°C / 83.9°C, 합성 모델 결과 |
| CSV 다운로드 | 헤더 + 128개 위성 행 |
| 실험 저장 → 시간 진행 → 불러오기 | 15분 시점과 대응 이력 복원 |
| 평면도 전환·위성 선택 | 선택 위성 006 반영 |
| 가시성 필터 | 요약 가시 위성 수와 목록 수 일치 |
| 4개 면 × 8기 설정 적용 | 32기로 초기화 |
| 수동 장애 주입 | 이벤트 및 관측값 갱신 |
| 시작·일시정지 | 모의 시간 진행·정지 |
| 모바일 및 안내 대화상자 | 가로 넘침 없음, 열기·닫기 동작 |
| 페이지 JavaScript 및 콘솔 오류 | 0개 |

화면 이미지는 `preview.png`에 포함했습니다. 스크린샷은 모의 대응 예제이며 실제 위성 자료가 아닙니다.

## 성능 참고

로컬 Python 프로세스에서 초기 학습 후, 512기·최근 20분 이력을 포함한 스냅샷을 5회 계산한 평균은 약 **36ms**, 관측된 프로세스 최대 메모리는 약 **138MB**였습니다. 이 값은 참고치이며 Render의 공유 CPU, 네트워크, 동시 접속, 런타임 조건에서 같은 성능을 보장하지 않습니다. Render 현장 부하 테스트는 수행하지 않았습니다.

## 배포 설정 확인

`render.yaml`의 YAML 파싱, Python 런타임, 빌드 명령, `main:app` 시작 경로, `$PORT`, `/healthz`, `.python-version`을 확인했습니다. Render의 인증된 Blueprint 검증 API나 실제 배포는 수행하지 않았습니다.

## 범위

검증 결과는 구현 일관성과 실험 흐름을 확인한 것입니다. 비행 데이터 기반 AI 정확도, 실제 고장 진단, 배터리·열·제어 물리 모델 정확도, 위성망 가용도, 실제 궤도예측 성능을 입증하지 않습니다. 세부 가정은 README를 참고하세요.
